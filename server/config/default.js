const config = {
  // 启动端口
  port: 8891,

  // 数据库配置
  database: {
    DATABASE: 'project_stat',
    USERNAME: 'root',
    PASSWORD: '123456',
    PORT: '3306',
    HOST: 'localhost'
  }
}

module.exports = config
